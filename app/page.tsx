'use client';
import { useState } from 'react'; import { ShieldCheck, Trophy, ArrowRight } from 'lucide-react'; import { useRouter } from 'next/navigation';
export default function Home(){const [token,setToken]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);const r=useRouter();async function login(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const res=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token})});const j=await res.json();setLoading(false);if(!res.ok)return setError(j.error);r.push('/dashboard');r.refresh();}return <main className="shell"><div className="container"><nav className="nav"><div className="brand"><span className="ball"><Trophy size={21}/></span> Campus Football</div><span className="muted">Official Team Registration Portal</span></nav><section className="hero"><div><span className="eyebrow"><ShieldCheck size={15}/> Secure registration</span><h1>Build your squad.<br/>Represent your department.</h1><p>Register your team and a complete 20–25 player squad. Your department is identified securely from your assigned competition token.</p></div><form className="card login-card" onSubmit={login}><div className="kicker">Department access</div><h2 style={{fontSize:28,margin:'9px 0 4px'}}>Enter your team token</h2><p className="muted" style={{lineHeight:1.6}}>Use the token issued to your department. The department name will be loaded automatically.</p><label className="label">Registration token</label><input className="input" placeholder="e.g. CSC-3286" value={token} onChange={e=>setToken(e.target.value.toUpperCase())} required autoCapitalize="characters"/><button className="btn full" style={{marginTop:14}} disabled={loading}>{loading?'Verifying…':<>Continue to registration <ArrowRight size={17} style={{verticalAlign:'middle'}}/></>}</button>{error&&<div className="error">{error}</div>}<div className="hint" style={{marginTop:18}}>Tokens are department-specific. Keep yours private and only share it with your team registration representative.</div></form></section><div className="footer-note">Designed for phones, tablets and desktop screens.</div></div></main>}
